{
  "name": "guessdex",
  "module": "server/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run server/index.ts",
    "dev": "bun run --watch server/index.ts",
    "build": "cd client && bun run build && cd .."
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
